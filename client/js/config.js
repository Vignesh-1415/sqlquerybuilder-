// Single place to point the frontend at a backend. When you deploy the
// server (Render, Railway, etc.), change apiBaseUrl to that URL — nothing
// else in the frontend needs to change.
window.STATEMENT_CONFIG = {
  apiBaseUrl: 'http://localhost:4000/api',
};
